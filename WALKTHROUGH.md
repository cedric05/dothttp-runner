```
"walkthroughs": [
			{
				"id": "dothttpStart",
				"title": "Start making Http Requests",
				"description": "Your first steps to set up dothttp-runner, request executor!",
				"steps": [
					{
						"id": "dothttp.start",
						"title": "Configure Dothttp path",
						"description": "The built-in downloader makes it easy to set up dothttp path.\n[Run Command](command:getting-started-sample.runCommand)",
						"media": {

						}
					},
					{
						"id": "dothttp.runRecent",
						"title": "Toggle (or) configure rerun mostly recently ran target in httpfile",
						"description": "Toggle (or) configure rerun mostly recently ran target in httpfile. \n[Configure Command](command:dothttp.command.toggle.runrecent)"
					}
				]
			}
		],
```